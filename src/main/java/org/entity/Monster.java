package org.entity;

public class Monster extends Entity {
    public Monster(String name, int health, int attack, int protection, int minDamage, int maxDamage) {
        super(name, health, attack, protection, minDamage, maxDamage);
    }
}
